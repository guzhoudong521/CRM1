<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="crm.dao.IUsersDao">

    <select id="getAllUser" parameterType="queryParam" resultType="users">
       select * from  (select u.*,rownum r from users u where 1=1
        
             <if test="gonghao!=null">
               and  u.userid=#{gonghao}
             </if>
             <if test="name!=null">
               and  u.uname like #{name}
             </if>
        ) where r between  (#{page}-1)*#{pageSize} and #{page}*#{pageSize}
    </select>
    
    <select id="getCount" parameterType="crm.util.QueryParam" resultType="int">
       select count(0) from users where 1=1
  
             <if test="gonghao!=null">
               and   userid=#{gonghao}
             </if>
             <if test="name!=null">
              and   uname like #{name}
             </if>
       
    </select>
</mapper>
